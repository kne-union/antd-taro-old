import React from 'react'
import { Switch as TaroSwitch } from '@tarojs/components'

const Switch = (props)=>{
  return <TaroSwitch {...props} color="var(--adm-color-primary)"/>
};

export default Switch;
